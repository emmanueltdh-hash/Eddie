# Eddie's Birthday — flat upload version

Upload every file in this folder directly to the root of the GitHub repository.
The build script automatically creates the folders required by Next.js and Vercel.
